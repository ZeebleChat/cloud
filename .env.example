# ── Application URLs ──────────────────────────────────────────────────────────
ZCLOUD_PUBLIC_URL=https://cloud.example.com
CORS_ORIGIN=http://localhost:5173

# ── Voice / stream ─────────────────────────────────────────────────────────────
REDIS_URL=redis://127.0.0.1:6379
# Comma-separated origins allowed to open the voice WebSocket (empty = deny all browser origins)
VOICE_ALLOWED_ORIGINS=http://localhost:5173

